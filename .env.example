# The port on which the SSE bridge server runs (defaults to 3001)
PORT=3001

# Optional: If you want to use the cloud Anthropic API directly instead of the local Claude CLI session
# ANTHROPIC_API_KEY=your_api_key_here
